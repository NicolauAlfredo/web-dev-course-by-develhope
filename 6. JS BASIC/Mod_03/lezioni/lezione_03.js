/* Lezione 03 - L'operatore Ternario */

let isRaining = true;
let isCloudy = false;

let result = isRaining
  ? "I should take an umbrella!"
  : isCloudy
    ? "I could take an umbrella!"
    : "It's a sunny day!";

console.log(result);
