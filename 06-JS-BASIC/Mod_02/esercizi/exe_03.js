const rectangleWidth = 2;
const rectangleHeight = 6;

const rectangleArea = rectangleWidth * rectangleHeight;

console.log(
  "L'area di un rettangolo di base: " +
    rectangleWidth +
    " e altezza: " +
    rectangleHeight +
    " è uguale a: " +
    rectangleArea,
);
