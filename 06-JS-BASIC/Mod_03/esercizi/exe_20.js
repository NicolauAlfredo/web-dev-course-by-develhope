const isSunnyDay = true;
const isHomeworkCompleted = true;

// Utilizzo dell'operatore ternario per verificare entrambe le condizioni
const result =
  isSunnyDay && isHomeworkCompleted
    ? "Jesse can go out to play"
    : "Jesse stays at home";

console.log(result);